# Base URL of the API. In dev, leave as /api/v1 and Vite proxies to the backend.
VITE_API_URL=/api/v1

# Dev-only: where Vite proxies /api during `npm run dev`
VITE_PROXY_TARGET=http://localhost:4000
